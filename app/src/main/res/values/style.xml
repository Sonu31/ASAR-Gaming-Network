<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ASARGamingNetwork" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="android:windowBackground">@color/appbg</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:fontFamily">@font/font</item>
        <item name="android:statusBarColor">@color/black</item>
        <item name="android:navigationBarColor">@color/black</item>

    </style>
    <style name="PopMen" parent="Widget.AppCompat.PopupMenu.Overflow">
        <!-- change menu text color-->
        <item name="android:textColor">@color/white</item>
        <!--change popup menu background color-->
        <item name="popupMenuBackground">@color/_000000</item>

        <item name="android:radius">30dp</item>
        <item name="cardCornerRadius">30dp</item>
        <item name="cornerRadius">30dp</item>
    </style>

    <style name="CustomShapeAppearance2">
        <!--        <item name="neumorph_cornerSize">32dp</item>-->

        <!-- Or if wants different radii depending on the corner. -->
        <item name="neumorph_cornerSizeTopLeft">18dp</item>
        <item name="neumorph_cornerSizeTopRight">18dp</item>
        <item name="neumorph_cornerSizeBottomLeft">18dp</item>
        <item name="neumorph_cornerSizeBottomRight">18dp</item>
    </style>
    <style name="CustomShapeAppearance3">
        <!--        <item name="neumorph_cornerSize">32dp</item>-->

        <!-- Or if wants different radii depending on the corner. -->
        <item name="neumorph_cornerSizeTopLeft">11dp</item>
        <item name="neumorph_cornerSizeTopRight">11dp</item>
        <item name="neumorph_cornerSizeBottomLeft">11dp</item>
        <item name="neumorph_cornerSizeBottomRight">11dp</item>
    </style>
    <style name="CustomShapeAppearance">
        <!--        <item name="neumorph_cornerSize">32dp</item>-->

        <!-- Or if wants different radii depending on the corner. -->
        <item name="neumorph_cornerSizeTopLeft">20dp</item>
        <item name="neumorph_cornerSizeTopRight">20dp</item>
        <item name="neumorph_cornerSizeBottomLeft">20dp</item>
        <item name="neumorph_cornerSizeBottomRight">20dp</item>
    </style>
</resources>